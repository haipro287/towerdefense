package towerdefense.entity.tile.road;

public class Target extends Road {
    public Target(long createdTick, int x, int y, int width, int height) {
        super(createdTick, x, y, width, height);
    }
}
