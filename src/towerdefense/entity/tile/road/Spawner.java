package towerdefense.entity.tile.road;

public class Spawner extends Road {
    public Spawner(long createdTick, int x, int y, int width, int height) {
        super(createdTick, x, y, width, height);
    }
}
